query assets {
  assets(first: 1000, orderBy: symbol, orderDirection: asc) {
    id
    name
    symbol
    decimals
    trackingVaults {
      id
      name
      owner {
        id
      }
      trackedAssets {
        id
        name
        symbol
        decimals
      }
    }
  }
}
