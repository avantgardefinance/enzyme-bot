query vault($id: ID!) {
  vault(id: $id) {
    id
    name
    deposits {
      id
      shares
    }
    trackedAssets {
      id
      name
      decimals
      symbol
    }
    release {
      id
    }
    comptroller {
      id
      creator {
        id
      }
    }
  }
}
